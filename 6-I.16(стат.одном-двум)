#include <iostream>
using namespace std;

int main()
{
    int n = 10;
    int a[10];
    for (int i = 0; i < n;i++)
    {
        a[i] = i;
    }
    int x, y;
    cin >> x >> y;
    for (int f = 0; f < n; f++)
    {
        if (a[f] < x || a[f] > y)
        {
            cout << f << endl;
        }
    }
}



#include <iostream>
using namespace std;

int main()
{
    const int n = 3;
    const int g = 10;
    int a[n][g];
    for (int i = 0; i < n;i++)
    {
        for (int z = 0;z < g;z++)
        {
            a[i][z] = z;
        }
    }
    for (int i = 0; i < n;i++)
    {
        for (int z = 0;z < g;z++)
        {
            cout << a[i][z] << endl;
        }
    }
    cout << endl;
    int x, y;
    cin >> x >> y;
    for (int i = 0; i < n;i++)
    {
        for (int z = 0;z < g;z++)
        {
            if (a[i][z] < x || a[i][z] >y)
            {
                cout << i << " " << z << endl;
            }
        }
    }
}
