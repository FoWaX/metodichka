#include <iostream>
#include <windows.h>
using namespace std;

int main()
{
	SetConsoleCP(1251);
	SetConsoleOutputCP(1251);
	int x, y;
	cin >> x >> y;
	if (!(x >= -15 && x <= 0 && y>=-15 && y <= 0))
	{
		cout << "Да";
	}
	else if (x > -15 && x<0 && y>-15 && y < 0)
	{
		cout << "Нет";
	}
	else 
	{
		cout << "На границе";
	}
}
