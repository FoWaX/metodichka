#include <iostream>
using namespace std;

int main()
{
    const int n = 5;
    int a[n];
    int f;
    for (int i = 0; i < n-1;i++)
    {
        int q;
        cin >> q;
        a[i] = q;
    }
    for (int i = 1; i < n-1;i++)
    {
        if (a[i] < 0)
        {
            for (int z = n-1;z > i-1;z--)
            {
                 a[z] = a[z-1];
            }
            cin >> f;
            a[i] = f;
            break;
        }
    }
    for (int i = 0; i < n;i++)
    {
        cout << a[i] << " ";
    }
    
}


