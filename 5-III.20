
#include <iostream>
#include<cmath>
using namespace std;

int main()
{
	double e,s,i;
	s = 0;
	i = 1;
	e = 0.000001;
	while (1. / sqrt(pow(3, i)) > e)
	{
		s += 1. / sqrt(pow(3, i));
		i++;
	}
	cout << s;
}

