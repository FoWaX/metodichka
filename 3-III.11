#include <iostream>
using namespace std;

int main()
{
	int a, b;
	cin >> a >> b;
	int x, y;
	cin >> x >> y;
	for (;a <= b;a++)
	{
		if (a % 10 == x || a % 10 == y)
		{
			cout << a << endl;
		}
	}
}


#include <iostream>
using namespace std;

int main()
{
	int a, b;
	cin >> a >> b;
	int x, y;
	cin >> x >> y;
	while (a <= b)
	{
		if (a % 10 == x || a % 10 == y)
		{
			cout << a << endl;
		}
		a++;
	}
}



#include <iostream>
using namespace std;

int main()
{
	int a, b;
	cin >> a >> b;
	int x, y;
	cin >> x >> y;
	do
	{
		if (a % 10 == x || a % 10 == y)
		{
			cout << a << endl;
		}
		a++;
	} while (a <= b);
}
