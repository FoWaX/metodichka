#include <iostream>
using namespace std;

int main()
{
	int a, b;
	cin >> a >> b;
	int x, y;
	cin >> x >> y;
	for (;a <= b;a++)
	{
		if (a % 10 == x || a % 10 == y)
		{
			cout << a << endl;
		}
	}
}
